# Google Cloud Storage Configuration
# Add this variable to your .env file.
# On GCE, credentials are auto-discovered from the VM-attached service account.

# Private bucket used for resume uploads
GCS_BUCKET_NAME=talentswype-resume-bucket
